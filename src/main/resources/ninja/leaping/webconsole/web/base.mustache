<!DOCTYPE html>
<html>
<head>
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ basedir }}/static/style.css" type="text/css" />
    <meta id="basedir" name="{{ basedir }}">
</head>
<body>
<nav>
    <ul>
        {{#user }}
            <li>Welcome, {{ name }}!</li>
            <li><a href="{{ basedir }}/logout">Log out</a></li>

        {{/user}}
        {{^user }}
            <li><a href="{{ basedir }}/login">Log in</a></li>
        {{/user}}
    </ul>
</nav>
<main>
    {{$ content }}
        <strong>Template error! The admin has not set any content!</strong>
    {{/ content }}
</main>
</body>
</html>
